# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[plugins]

# Initialize the completion system before plugins that register completions
# via compdef. `-i` skips the insecure-directory prompt (nix fpath dirs are
# often group-writable).
[plugins.compinit]
inline = 'autoload -Uz compinit && compinit -i'

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"

[plugins.fast-syntax-highlighting]
github = "zdharma/fast-syntax-highlighting"

# just dynamic completion: reads recipes from the justfile in the current
# directory at completion time. Loaded last so it runs after compinit and
# fast-syntax-highlighting.
[plugins.just]
inline = 'command -v just > /dev/null 2>&1 && eval "$(just --completions zsh)"'

# For example:
#
# [plugins.base16]
# github = "chriskempson/base16-shell"
