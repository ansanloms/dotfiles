---
# このテンプレートの案内コメントは、成果物では削除する。
# ステータス。proposed / accepted / rejected / deprecated / superseded のいずれか。
# 新規作成時は proposed とする (合意済みの決定でも、合意の記録は proposed からの遷移で行う)。
status: proposed

# 決定日時 (ISO 8601 の UTC 表記。例: 2021-05-22T00:00:00Z)。起票時は起票日時を書き、accepted / rejected へ遷移させた日時で更新する。
date: YYYY-MM-DDThh:mm:ssZ

# この ADR が置き換える ADR の ID (数値の配列)。無いなら削除する。
supersedes: []

# この ADR を置き換えた ADR の ID (数値の配列)。相手側の status を superseded にするときに相互に設定する。無いなら削除する。
superseded-by: []

# 関連する ADR の ID (数値の配列)。本文で参照した ADR はここにも宣言する。参照が無いなら削除する。
# supersedes / superseded-by に宣言した相手はここに重複させない。
refs: []

# 関連する語のタグ。不要なら削除する。
tags: []
---

<!--
ADR (Architecture Decision Record) のテンプレート。

使い方:
- このファイルを ADR の配置先 (既定 docs/adr/) に NNNN-<短い要約>.md としてコピーして書く。NNNN は 4 桁ゼロ埋めの連番。欠番は再利用しない。
- ファイル名の要約は英語またはローマ字の kebab-case (ASCII) で書く。例: 0001-use-postgresql-as-primary-store.md
- 本文は日本語で、通常の文書体で書く。
- 案内コメント (frontmatter 内の # 行と、この HTML コメントを含む) は、書き終えたらすべて削除する。

ADR にするものの閾値 (いずれかに当たるもの):
- 層の境界・データモデル・外部依存 (ライブラリ・フレームワーク・外部サービス・ライセンス) に触れる決定
- 覆すと既存コードの書き直し、または保存済みデータの移行が要る決定
- 「やらない」と決めたこと (スコープの切り捨て)
命名規約やフォーマッタ設定など、変えても影響が局所的なものは ADR にしない。

status の遷移: proposed -> accepted | rejected、accepted -> deprecated | superseded、deprecated -> superseded
- deprecated は撤回が決まったが完了していない状態 (後続 ADR が未合意、または移行が未完了)、superseded は撤回が完了した状態。
- 本文 (Context / Decision / Consequences 等) を変更できるのは proposed の間だけ。
  決定を覆すときは新しい ADR を書き、新側の supersedes と旧側の superseded-by を相互に更新する。
- Assumptions の「状態」列は例外で、検証が進んだら更新してよい (事実の追記であり、決定の変更ではない)。
-->

# ADR-NNNN: <決定の要約。「〜する」「〜しない」の形で 1 行>

## Context

<!--
決定を迫った状況。何が問題で、なぜ今決める必要があるのか。
事実だけを書き、ここに結論や評価を混ぜない。
-->

## Decision Drivers

<!--
判断基準を優先順に列挙する。件数は得られた情報に合わせる (1 件でもよい)。
例: 外部サービスの障害時にも書き込みを受け付け続ける必要がある
-->

1.
2.

## Considered Options

<!--
検討した選択肢をすべて残す。却下したものにも 1〜2 行で「なぜ落としたか」を書く。
採用した選択肢には「採用」と明記する。件数は実際に検討したものに合わせる (2 件でもよい)。
-->

1. <選択肢 A> — 採用。<理由>
2. <選択肢 B> — 却下。<理由>
3. <選択肢 C> — 却下。<理由>

## Decision

<!--
採用した選択肢と、それに伴い守るべき制約を箇条書きにする。
「〜する」「〜しない」の形で、後からコードと機械的に照合できる粒度にする。
-->

-
-

## Consequences

### 利点

-

### 代償

<!--
この決定で背負う実装・運用コスト。隠さず書く。
完了すれば嘘になる記述 (作業予定・一時的な移行状態等) は書かず、恒久的なコストだけを書く。
-->

-

### 禁止事項

<!--
この決定が成立するために、以後やってはいけないこと。
例: ui 層が core 層の関数を直接呼ぶこと (メッセージ境界を迂回すること)
-->

-

## Assumptions

<!--
この決定が依存する前提と、その検証状況。前提が崩れたらこの ADR を見直す。
状態は「未検証」「検証済」「棄却」のいずれか。棄却された前提がある場合、この ADR を supersede する ADR を検討する。
-->

| 前提   | 状態   | 確認方法 / 結果                        |
| ------ | ------ | -------------------------------------- |
| <前提> | 未検証 | <どう確認するか。確認したら結果と日付> |

## References

<!--
出典を列挙する。URL・リポジトリ内のファイルパス・コミットハッシュ・会話ログ等。
issue・PR は裸の番号 (#123) ではなく URL で書く。
出典なしに書いた推測は、推測であることを明記する。
-->

-
